# UI Shells

This folder contains frontend shell starters that can be scaffolded by `dotnet new` with:

- `--ui angular`
- `--ui react`
- `--ui mvc`
- `--ui none` (default)

These shells are contract-first starters. UI state should come from backend `ui-manifest` and generated API clients.

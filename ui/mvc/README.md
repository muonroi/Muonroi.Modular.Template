# MVC Shell Starter

This shell is an ASP.NET Core MVC starter to consume Muonroi UI Manifest from backend API.

Use cases:

- Server-side rendering/BFF style app.
- Corporate intranet portals.
- Transitional UI while SPA is being built.

Backend endpoint expected:

- `GET /api/v1/auth/ui-manifest/{userId}`
